# Copy to .env for direct, non-Docker development only.
# This URL assumes local peer/trust authentication. If your PostgreSQL requires
# a password, add a locally generated one only to the ignored .env file.
DATABASE_URL=postgresql://unified_commerce@localhost:5432/unified_commerce?schema=public
PORT=4000
UPLOADS_DIR=../../uploads
CORS_ORIGINS=http://localhost:3000

